export interface Admin {
  fullName: string;
  email: string;
  contactNo: string;
  address: string;
}
export interface Service {
  name: string;
  price: string;
  description: string;
}
export interface Category {
  title: string;
}
